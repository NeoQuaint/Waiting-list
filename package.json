{
  "name": "waitlist-app",
  "version": "1.0.0",
  "description": "Waitlist signup page with PostgreSQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test-db": "node -e \"require('dotenv').config(); const { testConnection } = require('./database.js'); testConnection();\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.11.2",
    "helmet": "^7.2.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
  "node": "18.x"
}

}
